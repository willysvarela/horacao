import React, { useState, useEffect } from "react";
import { View } from "react-native";
import PropTypes from "prop-types";

const PrayerDetailScreen = (props) => {
  return <View></View>;
};
PrayerDetailScreen.propTypes = {};

export default PrayerDetailScreen;
